apiVersion: v1
kind: Service
metadata:
  name: order-service
  annotations:
    beta.cloud.google.com/backend-config: '{"default": "order-backendconfig"}'
spec:
  selector:
    app: order-service  
  ports:
    - port: 80
      targetPort: 8080
